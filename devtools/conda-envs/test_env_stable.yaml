name: test-stable
channels:
  - conda-forge

dependencies:
  - psi4
  - qcfractal >= 0.56
  - qcportal >= 0.56
  - qcelemental
  - nwchem

    # Base depends
  - python
  - pip
  - pydantic

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

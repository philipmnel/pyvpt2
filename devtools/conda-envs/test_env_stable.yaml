name: test-stable
channels:
  - psi4
  - defaults
  - qcarchive/label/next
  - conda-forge
dependencies:
  - psi4
  - qcfractal=0.50b7
  - qcportal
  - qcelemental
  - intel-openmp
  
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov


name: test-stable
channels:
  - qcarchive/label/next
  - conda-forge
  - conda-forge/label/libint_dev

dependencies:
  - psi4
  - qcfractal
  - qcportal
  - qcelemental

    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

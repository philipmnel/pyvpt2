name: test-stable
channels:
  - conda-forge

dependencies:
  - psi4
  - qcfractal
  - qcportal < 0.56
  - qcelemental
  - nwchem

    # Base depends
  - python
  - pip
  - pydantic < 2

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

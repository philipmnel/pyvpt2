name: test
channels:
  - psi4
  - pkgs/main
  - conda-forge
dependencies:
  - psi4
  - qcelemental
  - intel-openmp
  
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov


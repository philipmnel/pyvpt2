name: test
channels:
  - psi4
  - conda-forge
dependencies:
  - psi4
  - qcelemental
  - intel-openmp!=2019.5
  
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov


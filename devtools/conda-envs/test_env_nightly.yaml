name: test-nightly
channels:
  - conda-forge/label/psi4_dev
  - conda-forge

dependencies:
  - psi4
  - qcfractal
  - qcportal < 0.56
  - qcelemental
  - nwchem

    # Base depends
  - python
  - pip
  - pydantic < 2

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov
